# Static site (repo root is the publish dir) + one tiny Netlify Function
# that serves runtime config (API_BASE) from environment variables, so it
# never needs to be hardcoded in index.html.
[build]
  publish = "."
  functions = "netlify/functions"

[build.environment]
  NODE_VERSION = "20"
