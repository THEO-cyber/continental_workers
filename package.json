{
  "name": "continental-workers",
  "private": true,
  "description": "Continental Auto Parts — workers PWA (static site + a tiny Netlify Function for runtime config)",
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@netlify/functions": "^3.0.0",
    "@types/node": "^20.14.0",
    "typescript": "^5.6.3"
  }
}
